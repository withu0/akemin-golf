; Local / shared-host upload limits for activity media (images 25MB, videos 100MB).
; Used by PHP-FPM / CGI when this directory is the docroot. For `php artisan serve`,
; prefer: php -d upload_max_filesize=128M -d post_max_size=128M artisan serve
upload_max_filesize = 128M
post_max_size = 128M
